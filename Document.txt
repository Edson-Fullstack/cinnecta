sudo apt-get install python3
sudo apt-get install pip3
pip3 install Flask  # /home/edson/.local/bin
pip3 install pep8 # checar codigo
pip3 install pymongo # database nosql
pip3 install pymongo[srv] # dnspy
python -c "import ssl; print(getattr(ssl, 'HAS_SNI', False))" # True
openssl version # 1.0>
pip install random-object-id
